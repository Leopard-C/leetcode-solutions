#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

#define Log(x) cout << (x) << endl

class Solution {
public:
    vector<vector<int>> permute(vector<int>& nums) {
        
    }
};

int main() {
	Solution s;

	Log("OK!");
}

