#include <algorithm>
#include <iostream>
#include <vector>
#include <string>
using namespace std;

#define Log(x) cout << (x) << endl

class Solution {
public:
    int evalRPN(vector<string>& tokens) {

    }
};

int main() {
	Solution s;

	Log("OK!");
}

